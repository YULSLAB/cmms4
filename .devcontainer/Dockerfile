FROM mcr.microsoft.com/devcontainers/java:21

# Install Gradle (optional if using wrapper)
RUN apt-get update && apt-get install -y gradle

# Install MariaDB client
RUN apt-get install -y mariadb-client
